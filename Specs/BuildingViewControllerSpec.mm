#import "BuildingViewController.h"

using namespace Cedar::Matchers;
using namespace Cedar::Doubles;

SPEC_BEGIN(BuildingViewControllerSpec)

describe(@"BuildingViewController", ^{
    __block BuildingViewController *buildingViewController;

    beforeEach(^{

    });
    
    it(@"should know the campus", PENDING);
    
    it(@"should have a building object", PENDING);
    
    it(@"should know the number of building to display", PENDING);
    
});

SPEC_END
